class CouponRepositoryTest : StringSpec({
    "CouponRepository should be testable in the future" {
        // Placeholder, add actual tests if needed.
        //  The only change to CouponRepository was adding a newline. So no tests are added for now.
        1 shouldBe 1
    }
})